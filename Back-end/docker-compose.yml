version: '4'
services:
  mysql:
    image: mysql:8.4.5
    container_name: swp-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - ./mysql:/var/lib/mysql